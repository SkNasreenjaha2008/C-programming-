#include<stdio.h>
#include<conio.h>
int main() {
    int number, digit, i = 0, j;
    int digits[10];
printf("Enter a number: ");
    scanf("%d", &number);
  if (number == 0) 
  {
        printf("The individual digit is: 0\n");
    }
    else
    {
        while (number > 0) 
        {
            digit = number % 10;
            digits[i] = digit;
            number = number / 10;
            i++;
        }

        printf("The individual digits are: ");
        for (j = i - 1; j >= 0; j--) {
            printf("%d ", digits[j]);
        }
        printf("\n");
    }
getch();
}


Output:
Enter a number:
54322.
The individual digit is :
5 4 3 2 2.
