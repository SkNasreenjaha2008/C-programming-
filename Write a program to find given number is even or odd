#include<stdio.h>
#include<conio.h>
int main() {
    int number;
    printf("Enter an integer ");
    scanf("%d", &number);
    if (number % 2 == 0) {
        printf("%d is an even number.\n", number);
    }
    else 
    {
        printf("%d is an odd number.\n", number);
    }
 getch();
}

Output:
Enter an integer:4
 4 is a even

