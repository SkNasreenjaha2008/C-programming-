#include<stdio.h>
void main()
{
  char name[30];
  int i, length=0;
  printf("Enter the name:");
  gets(name);
  for(i=0;name[i]!='\0';i++)
    {
      length++;
    }
  printf("Reverse :);
for(i=length-1;i>=0;i--);
printf("%d", name[i]);
    }


Output: 
Enter the name: pace 
 Reverse:ecap
